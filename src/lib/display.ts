export const borderThickness = '2px';

export const formatNumberToDisplayPrecision = (number: number): string => {
  return number.toFixed(0);
};

export const getEnergyUnit = (): string => {
  return 'kcal';
};
